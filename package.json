{
  "name": "badgehub-api",
  "version": "1.0.0",
  "description": "Node project for the BadgeHub API",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "start": "docker-compose up --detach",
    "stop": "docker-compose down",
    "build": "tsc",
    "dev": "concurrently 'tsc --watch' 'node --watch --env-file=.env dist/index.js'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "license": "MIT",
  "dependencies": {
    "express": "^4.19.2",
    "pg": "^8.11.5"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.12.7",
    "@types/pg": "^8.11.5",
    "concurrently": "^8.2.2",
    "typescript": "^5.4.5"
  }
}
